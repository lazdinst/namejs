export interface Components {
  navigationBar: {
    background: string;
    text: string;
    border: string;
    hover: string;
    active: string;
    accent: string;
  };
  panel: {
    background: string;
    border: string;
    text: string;
    shadow: string;
  };
  button: {
    primary: {
      background: string;
      text: string;
      border: string;
      hover: string;
    };
    secondary: {
      background: string;
      text: string;
      border: string;
      hover: string;
    };
  };
  card: {
    background: string;
    text: string;
    border: string;
  };
  tooltip: {
    background: string;
    text: string;
  };
  scrollbar: {
    thumb: string;
    track: string;
  };
}
